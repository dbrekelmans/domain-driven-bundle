services:
    DomainDrivenBundle\Routing\ContextRouteLoader:
        arguments:
            - '%domain_driven.directories.context%'
            - '%domain_driven.directories.infrastructure%/%domain_driven.directories.config%'
            - '%domain_driven.files.routes%'
        tags: [routing.loader]
